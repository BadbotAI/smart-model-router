# 项目记忆 · 模型路由平台（AI 交接文档）

> 目的：任何 AI/工程师读完本文档即可接手开发。**源码不在本仓库**——本仓库只是构建产物（GitHub Pages 站点）。源码、构建、通用代码约定见 `~/Desktop/富交互项目/项目记忆_智能交互平台.md`（先读它的第二~四、八~九章，本文档不重复）。
> 最后更新：2026-09-09（当前实现 = v7.0 智能路由方案：benchmark 画像 + 判维路由）

## 一、产品定位

B 端多模型智能路由平台：客户接入多个大模型，平台按「模型画像 × 成本-效果权重」为每次请求选最合适的模型（或聚合多模型答案）。**v7 核心思路：模型画像 = benchmark 成绩表 + 成本（静态配置、即改即生效、零冷启动）；每次路由由「智能路由模型」（独立接入的低延迟小模型）判定 query 相关的 benchmark 维度，候选取这些维度平均成绩融合省钱分路由**。与智能交互平台共用代码仓、独立站点与导航。线上：https://badbotai.github.io/smart-model-router/ ← 由 `富交互项目/build_docs.py` 构建输出到本目录。

## 二、页面与导航（源码在 富交互项目/web/）

导航按动线分两组（`ui.js` PLATFORMS.router）：
- **配置**：路由策略 `router.html#dispatch` / 模型画像 `#profile` / 模型接入 `#models`
- **运营**：模型路由测试 `playground.html#model` / 模型路由数据 `dashboard.html#routing`
- 首页 `home-router.html`（5 卡轮播 + 单 CTA，第五卡为智能判维演示）；操作日志共享 `audit.html`
- 旧深链 `#flow` / `#flywheel`（v6 数据集 / 数据飞轮页）自动跳 `#profile`

| 页面 | 职责（v7 现状） |
|---|---|
| 路由策略 | 策略卡（名称行/ID+API Key 脱敏块/摘要行）；三层示意条「1 硬规则 → 2 智能判维 → 3 else 兜底」；「模型路由规则」页头折叠块（5 条）；参数配置抽屉：名称/候选（tagSelect 带「· 图像」标注）/兜底/成本-效果权重滑杆/聚合开关/「响应终端用户的聚合选择」开关/「API 参数与返回说明」可复制折叠块（v7 已删 AB/feedback 字段）。新建即发 `sk-route-` Key |
| 模型画像 | v7 新页（替代数据集页+数据飞轮页）。三段：**智能路由模型配置条**（judge-bar 样式；未配置=红色警示「线上请求一律直连兜底模型，下表成绩不生效」+配置按钮；已配置可更换/移除）→ **Benchmark 成绩表**（行=模型含单价列，列=7 维度带 tooltip 释义；每格可点：修正 0-100 / 留空标缺失 / 恢复榜单值；修正格下划线标识；「刷新榜单快照」保留修正）→ **路由效果**（按策略下拉即时换算：每维度一行各模型综合分+路由去向 chip，tooltip 显示成绩/省钱/修正标记）。页头「画像规则」折叠块 4 条 |
| 模型接入 | 表：供应商/名称/型号/接入方式（API 官网单价 vs 自有部署按卡数估算）/单价/多模态/思考模式/启停/默认兜底；添加与编辑带图像识别、卡数字段。接入即生效（无入池评测），benchmark 缺分去画像页补录 |
| 模型路由测试 | TestChat keepReasoning 模式：路由策略选择器 + 聚合参数三段选择（模拟客户端 aggregate 传参）；回复四层：思考折叠→路由决策 details（策略/层级+判定维度列表/推导/模型/成本）→正文→底部路径标签；预设问法按维度设计（复合任务演示双维判定） |
| 模型路由数据 | 成本用量/路由行为（聚合率/快车道/多样性/**兜底与降级**）/聚合效果三复合卡 + 路由分布/失败降级/成本走势/延迟按路径。数据飞轮卡与探索流量指标已下线；历史 trace 的 explore 路径标「随机探索（历史）」 |

## 三、核心业务逻辑（server/，v7 现状）

**三层路由**（`router_core.run_route`，v7 重写）：
1. **硬规则层**：`mockmodels.classify_dimension` 判 multimodal（只在 capabilities.vision 模型中选，无则兜底并说明能力限制）/ chat（轻量直答；问候带真实任务不算闲聊）
2. **智能判维层**：`get_router_model()`（kv `router_model_info`）**硬依赖**——未配置 → `route_layer="no_router"` 兜底直连，事件与决策摘要说明原因（文案引导去「模型画像」页配置）；已配置 → `mockmodels.classify_bench_dims(query)` 判定相关维度（最多 2 个，multimodal 命中优先，兜底 knowledge），emit `{"step":"dims", dims}` + `router_judge` span（演示为关键词规则，生产替换为真实小模型结构化输出）→ `score_models(models, dims, alpha)`：各模型取判定维度非空平均分 /100 为 perf（全缺分退出候选），价格倒数归一为 eff，综合分 = α·perf + (1-α)·eff → 排序；gap ≤ max(0.02, (1-t)×0.3) 且 allow_aggregation → 并发 top2 聚合（聚合器 = params.aggregator_model 或 top1），否则快车道直答
3. **else 兜底**：判定维度全缺分 / 候选异常 → 兜底直连；不重入已失败候选
- 请求级 `aggregate=on/off/auto`（on 受 `params.allow_agg_override` 硬约束；配额降级撤销 force_agg）；决策记录 `dimensions`（列表）+ `dim_scores`

**benchmark 画像**（`router_core.get_bench_profile`）：`mockmodels.BENCH_SNAPSHOT`（7 维快照种子，部分模型 multimodal=None）+ kv `benchmark_overrides`（手动修正，格级覆盖）+ kv `benchmark_asof`（刷新日期）。**BENCH_DIMS 7 维**：knowledge/MMLU、math/GSM8K、coding/HumanEval、writing/WritingBench、instruct/IFEval、chinese/C-Eval、multimodal/MMMU。

**关键 API**（管理面 /api，对外 /v1）：
- `GET /api/benchmark`（dims+scores+models+overrides+asof+source+router）
- `POST /api/benchmark/score`（{model_id, dim, score|null}，0-100 校验，null=缺失）/ `POST /api/benchmark/score/reset` / `POST /api/benchmark/refresh`（更新 asof、保留修正）
- `GET/POST /api/settings/router-model`（judge 同款校验：显示名必填、ID `[a-z0-9][a-z0-9-]{1,23}`、https 接口、vault:// 凭证；空 model_id=移除）
- `GET /api/profile?policy_id=&alpha=`（从 benchmark 即时换算：rows=7 维度，generated 恒 true）
- `/v1/route`（SSE，final 带 request_id/applied/decision_summary[dimensions]）、`/v1/models`（CRUD+set-default/status/thinking/profile-data 含 deploy_type/gpu_count/vision）、`/v1/policies`（CRUD+duplicate）
- **v7 已删**：dataset pool/versions/rollback/cluster(+status)/query-delete、profile/matrix、profile/generate(+status)、settings/judge-model、settings/ab-sampling、/v1/feedback(+pending)、/api/flywheel
- 迁移 `seed.migrate_bench_v7`（kv `v7_bench` 幂等闸）：清 bank_responses、collected/reflow queries、dataset_versions、ab_feedback、聚类与画像 kv；seed_query_pool/seed_ab_feedback 停灌

**模拟层**：`mockmodels.py`——MODEL_POOL 6 模型（hash 播种确定性作答）、`BENCH_DIMS/BENCH_DIM_KEYWORDS/classify_bench_dims/BENCH_SNAPSHOT`、`classify_dimension`（硬规则用）；真实化只需改 `call_model()` 与 `classify_bench_dims()`

## 四、演示初始态与恢复

初始态 = **未配置智能路由模型**（动线：进模型画像页 → 看警示 → 配置路由模型 → 测试页看判维路由；两三下点完，零冷启动是卖点）。测试污染恢复：`POST /api/settings/router-model {"model_id":""}` 移除路由模型；kv 删 `benchmark_overrides`/`benchmark_asof` 恢复快照原值；测试产生的 trace 无需清（无落池）。历史脏模型 trace 已于 2026-09-09 清理（asdfasdfasdf 23 条）。

## 五、静态站 mock（docs/mock_api.js，构建时拷到本仓库）

会话内状态机 `v7 = {router, overrides, asof}` + `modelLocal/prodLocal`——静态站可走完整动线（未配置 no_router 兜底 → 配置路由模型 → 判维路由（关键词与服务端同规则）→ 修正分数 → 效果表联动）。数据基座 = 构建快照 `D["/api/benchmark"]`，overrides 会话内叠加。**行为单测**：`node 富交互项目/tests/mock_smoke.mjs`（v7 26 组断言，每轮必跑）。历史事故：mock 曾层积 5 份重复 postMock 靠后定义覆盖侥幸运行——改 mock 后必须跑单测；2026-09-09 又因 python 补丁锚点撞车（getMock 与 postMock 同名分支）导致区段重复，锚点必须限定搜索起点。

## 六、方案演进史（为什么是现在这样）

1. v3.x-v4.x：业务场景题库 + LLM 裁判打分 + 场景簇匹配；策略即 API；双平台拆分
2. v5.0（9/7 算法对齐会）：砍 LLM 裁判 → 能力维度客观题（QA 对+标准答案判分）+ AB 数据飞轮 + 一键演化
3. v5.1（9/7 外部评审 22 条质疑）：可信度整改 + 数据集版本化/回滚 + 回流接口化显式导入
4. v6.0（9/8 用户拍板）：废预置数据集 → 随机探索收集真实 query + 聚类定版 + Judge 回放 + 采纳自适应融合
5. **v7.0（9/9 老板拍板，已实施）**：数据集方式太复杂 → **benchmark 成绩表 + 成本 = 画像（静态配置零冷启动）+ 智能路由模型判维**。用户三点定夺：砍掉飞轮；维度固定 7 个；路由模型硬依赖。诚实代价：每次路由多一跳判维调用（决策中明示延迟与成本）；benchmark ≠ 业务成绩，靠手动修正兜底

## 七、记账清单（未做）

- 效果验证闭环缺位：飞轮砍掉后没有「benchmark 排序 vs 实际采纳」的偏差观测，将来可做轻量提示引导手动修正
- 判维演示实现为关键词规则，与生产（真实小模型结构化输出）行为可能有偏差；判维只取前 2 维是拍的
- `/api/benchmark/refresh` 演示只更新日期，生产需真实拉榜单 + diff 展示
- 路由模型自身的健康监控（判维超时/失败的降级路径未细化，当前异常直接走兜底）
- 策略并发写 last-write-wins；策略历史/回滚有接口无界面；/v1 与 /api 边界历史包袱；/v1/route 无同步（非 SSE）模式
- 多模态判定仍是词面（生产应按附件是否存在）
- 决策 payload 中 legacy 字段（support/coarse/fine 命名）残留无害但可清
