# 项目记忆 · 模型路由平台（AI 交接文档）

> 目的：任何 AI/工程师读完本文档即可接手开发。**源码不在本仓库**——本仓库只是构建产物（GitHub Pages 站点）。源码、构建、通用代码约定见 `~/Desktop/富交互项目/项目记忆_智能交互平台.md`（先读它的第二~四、八~九章，本文档不重复）。
> 最后更新：2026-09-09（当前实现 = v6.0 随机探索方案；**v7 智能路由方向已定待实施**，见第八章）

## 一、产品定位

B 端多模型智能路由平台：客户接入多个大模型，平台按「模型画像 × 成本-效果权重」为每次请求选最合适的模型（或聚合多模型答案）。与智能交互平台共用代码仓、独立站点与导航。线上：https://badbotai.github.io/smart-model-router/ ← 由 `富交互项目/build_docs.py` 构建输出到本目录。

## 二、页面与导航（源码在 富交互项目/web/）

导航按动线分两组（`ui.js` PLATFORMS.router）：
- **配置**：路由策略 `router.html#dispatch` / 数据集 `#flow` / 模型接入 `#models`
- **运营**：数据飞轮 `#flywheel` / 模型路由测试 `playground.html#model` / 模型路由数据 `dashboard.html#routing`
- 首页 `home-router.html`（5 卡轮播动画 + 单 CTA）；操作日志共享 `audit.html`

| 页面 | 职责（v6 现状） |
|---|---|
| 路由策略 | 策略卡（名称行/ID+API Key 脱敏块/摘要行）；三层示意条；「模型路由规则」页头折叠块；参数配置抽屉：名称/候选（tagSelect 带「· 图像」标注）/兜底/成本-效果权重滑杆/聚合开关/「响应终端用户的聚合选择」开关/「API 参数与返回说明」可复制折叠块。新建即发 `sk-route-` Key |
| 数据集 | 三段叙事：冷启动进度卡（问题池 N/500 → 亮「归类生成数据集 v1」；有版本后变版本下拉条+重新归类）→ 问题池（最近收集只读表+删除）→ 问题分类快照（AI 标签）→ Judge 配置条 + 路由效果（「生成模型画像」带成本预估 + 按策略 α 即时换算表，分数格 tooltip 显示 Judge/采纳构成） |
| 数据飞轮 | AB 监控：未入版本/累计回流、采样率滑杆、各模型采纳胜率（样本不足标注）、最近回流（时间范围下拉 + 「数据接口」双向对接弹窗）；页头「数据飞轮规则」折叠块 |
| 模型接入 | 表：供应商/名称/型号/接入方式（API 官网单价 vs 自有部署按卡数估算）/单价/多模态/思考模式/启停/默认兜底；添加与编辑带图像识别、卡数字段 |
| 模型路由测试 | TestChat keepReasoning 模式：路由策略选择器 + 聚合参数三段选择（模拟客户端 aggregate 传参）；回复四层：思考折叠→路由决策 details（策略/层级/推导/模型/成本/采样行）→正文→底部路径标签；主题化预设问法 |
| 模型路由数据 | 成本用量/路由行为/聚合效果三复合卡 + 数据飞轮卡 + 路由分布/失败降级/成本走势/延迟按路径（含「随机探索」） |

## 三、核心业务逻辑（server/，v6 现状）

**三层路由**（`router_core.run_route`）：
1. 硬规则层：`mockmodels.classify_dimension` 判 multimodal（只在 capabilities.vision 模型中选，无则兜底并说明）/ chat（轻量直答）
2. 分类匹配层：embedding 相似问题支撑集（`load_bank`，只吃已归入生效版本的数据）→ 粗排（label×confidence×时间衰减）→ 快车道或并发 K 候选 → 细排 → 聚合判定；**bank 为空（未归类）= 冷启动随机探索**：均匀 random.choice 直答，`route_layer="explore"`
3. else 兜底：无相似问题→兜底直连；兜底不重入已失败候选
- 决策记录 `route_layer/dimension`（dimension 用 `classify_theme` 主题）；请求级 `aggregate=on/off/auto`（on 受策略 `params.allow_agg_override` 硬约束控制；配额降级撤销 force_agg）

**数据流水线（v6）**：query 自动落池（`bank_queries source='collected'`，classify_theme 主题标签）→ 池满 500 `POST /api/dataset/cluster` 归类生成版本（`dataset_versions` + kv `dataset_clusters_v{n}`，AB 采纳随版本归入 `ab_feedback.imported_version`）→ `POST /api/profile/generate` 版本级画像（每分类 8 条代表问题 × 全模型模拟作答 × Judge 打分写回 `bank_responses[llm_judge]`，融合分类内 AB 采纳，**采纳权重 w=n_ab/(n_ab+20) 自适应**，存 kv `profile_matrix_v{n}`）→ `/api/profile` 按策略 α 即时换算 → 版本可回滚（`POST /api/dataset/rollback`，全链路按生效版本过滤）

**AB 数据飞轮**：`_handle_turn` 按 kv `ab_sampling_rate` 采样出 `ab_test` 双答案（画像期复用候选；**探索期额外补调一个模型**）；决策落库 `decision.ab_test`（feedback 只认真实出过 AB 的请求）；`POST /v1/feedback`（一票制）；`GET /v1/feedback/pending`（增量拉取）

**关键 API**（管理面 /api，对外 /v1）：`/api/dataset/overview|pool|versions|rollback|cluster(+status)|query/delete`、`/api/profile(+generate+status+matrix?version=)`、`/api/settings/judge-model|ab-sampling`、`/api/flywheel?days=&limit=`、`/v1/route`（SSE，final 带 request_id/applied/ab_test/decision_summary）、`/v1/feedback(+pending)`、`/v1/models`（CRUD+set-default/status/thinking/profile-data 含 deploy_type/gpu_count/vision）、`/v1/policies`（CRUD+duplicate，params 含 alpha/fallback_model/allow_agg_override）

**模拟层**：`mockmodels.py`——MODEL_POOL 6 模型带隐藏画像（hash 播种确定性作答）、`QUERY_THEMES` 6 业务主题（聚类隐藏真值，映射画像键）、`classify_theme/classify_dimension`；真实化只需改 `call_model()`

## 四、演示初始态与恢复

初始态 = 冷启动待归类：问题池 508/500 达标、无版本、无 Judge、无画像（用户三步走完全流程：配 Judge → 归类生成 → 生成画像）。测试污染后的恢复脚本（谨慎，先看再跑）：清 `bank_responses`、`bank_queries.dataset_version=NULL`、删测试收集行（`col-` 8 位随机 id）、清 `dataset_versions`、`ab_feedback.imported_version=NULL` 并删 `trace_id IS NOT NULL` 测试行、删 kv `dataset_clusters_v%/profile_matrix_v%/judge_model_info`、重启。

## 五、静态站 mock（docs/mock_api.js，构建时拷到本仓库）

会话内状态机 `v6 = {judge, version, generated, deletedQ}` + `modelLocal/prodLocal/flyLocal`——静态站可走完整动线（配 Judge→归类→画像→效果表→模型操作）。**行为单测**：`node 富交互项目/tests/mock_smoke.mjs`（13 组断言，每轮必跑）。历史事故：mock 曾层积 5 份重复 postMock 靠后定义覆盖侥幸运行——改 mock 后必须跑单测。

## 六、方案演进史（为什么是现在这样）

1. v3.x-v4.x：业务场景题库 + LLM 裁判打分 + 场景簇匹配；策略即 API；双平台拆分
2. v5.0（9/7 算法对齐会）：砍 LLM 裁判 → 能力维度客观题（QA 对+标准答案判分）+ AB 数据飞轮 + 一键演化
3. v5.1（9/7 外部评审 22 条质疑）：可信度整改 + **数据集版本化/回滚 + 回流接口化显式导入**（用户强调：数据的获取/流转/处理/版本/回退要在方案期想清）
4. v6.0（9/8 用户拍板）：废预置数据集 → **随机探索收集真实 query + 聚类 + Judge 回归 + 采纳自适应融合**；QA 对/上传导入/入池评测下线
5. **v7（9/9 老板拍板，待实施）**：见下章

## 七、v7 智能路由方向（已定，待三个拍板点后实施）

老板认为数据集方式太复杂，改为：**benchmark 分数表 + 成本 = 模型画像（静态配置即改即生效）**；每次路由由「智能路由模型」（小 LLM）判断 query 相关哪些 benchmark 维度 → 候选取这些维度**平均分** × α 路由；聚合保留。产品变化：删数据集页概念群/Judge/冷启动探索/画像生成任务（零冷启动是卖点）；新增「模型画像页」（维度×模型分数表，来源=榜单快照/手动修正，缺分跳过）与「智能路由模型」配置；测试页决策层显示判维结果；飞轮建议降级为「效果验证」（采纳胜率 vs benchmark 排序偏差 → 提示手动修正分数）。**待用户拍板**：①飞轮去向（降级观测 vs 砍掉）②维度集合与缺分处理 ③路由模型未配置时软降级（关键词判维）还是硬依赖。诚实代价（要写进方案）：每次路由多一跳 LLM 调用；benchmark≠业务成绩，靠效果验证+手动修正兜底。

## 八、记账清单（未做）

问题池无频控/来源鉴权（生产需）；已删模型历史采纳仍在胜率表；AB 平局/跳过选项缺失；策略并发写 last-write-wins；策略历史/回滚有接口无界面；/v1 与 /api 边界历史包袱；/v1/route 无同步（非 SSE）模式；多模态无独立画像（词面判定，生产应按附件）；实际 AB 采样率低于名义值（单路请求不出双答案）
